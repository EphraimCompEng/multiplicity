##############################################
# Extract Columns From Multiplied DataFrames #
##############################################

import multiplied as mp
import pandas as pd
import pyarrow as pa

def df_extract_bits(df: pd.DataFrame, bits: list[int], stages: list[int]) -> pd.DataFrame:
    """

    """
    ...

def df_extract_columns(df: pd.DataFrame, columns: list[str]) -> pd.DataFrame:
    """

    """
    ...

def df_extract_formatted_all(df: pd.DataFrame) -> pd.DataFrame:
    """

    """
    ...

def df_extract_formatted_columns(df: pd.DataFrame, stages: list[int]) -> pd.DataFrame:
    """

    """
    ...
